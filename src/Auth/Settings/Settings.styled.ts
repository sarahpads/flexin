import styled from "styled-components";

import theme from "../../theme";
export { H1, P, Link, Cancel } from "../../Layout/Typography.styled";

export const Settings = styled.div`
  padding: ${theme.dimensions.pagePadding};
`